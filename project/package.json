{
    "scripts": {
        "dev": "ts-node-dev --respawn --debug --rs --watch src/**/*.ts src/index.ts src/innerTest.ts",
        "build": "npm run build --prefix=../shared && tsc -b"
    },
    "devDependencies": {
        "@types/node": "^14.14.35",
        "ts-node": "^9.1.1",
        "ts-node-dev": "^1.1.6",
        "typescript": "^4.2.3"
    }
}
