{
    "scripts": {
        "dev": "ts-node-dev --respawn --debug src/index.ts",
        "build": "npm run build --prefix=../shared && tsc -b"
    },
    "devDependencies": {
        "@types/node": "^14.14.35",
        "ts-node-dev": "^1.1.6",
        "typescript": "^4.2.3"
    }
}
